access_key_id: access_key # aws access key to use for requests
secret_access_key: secret_key # aws secret key to use for requests
export_schema: schema # schema to load test data into
export_bucket: export-bucket # bucket to be used for exports
export_username: username # RedShift username to use for exports
export_password: password # RedShift password to use for exports

archive_schema: schema # schema to save archive info
archive_bucket: archive-bucket # bucket to be used for archiving
archive_username: username # RedShift username to use for archives
archive_password: password # RedShift password to use for archives
