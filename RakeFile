$: << File.join(File.dirname(__FILE__), 'app')

require 'bundler'
Bundler.require
require './app/app'

# activerecord tasks
require "sinatra/activerecord/rake"
namespace :db do
  task :load_config do
    require "./app/app"
  end
end
